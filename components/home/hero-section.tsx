"use client"

import React from 'react'
import HeroText from '@/components/home/hero-text'

const HeroSection = () => {
  return (
    <section>
        <HeroText />
    </section>
  )
}

export default HeroSection